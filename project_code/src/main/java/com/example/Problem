1.删除后id不会到上一个起点，而是继续自增
2.新增用户时，若用户名输错，则也id也自增
3.添加方法，在注册成功后自动添加到登录界面
4.个人信息界面修改信息后，user.nickname标识未更新
5.第一次登录后，个人信息和用户管理会出现权限未打开的情况
8.课程状态已修无法检索问题
9.课程老师无法通过编辑删除

11.清空筛选时，排序无法清空问题

15.在course界面设置。如果数据库有user.id = student_course.student_id，则不显示选课。

17.完整预览功能
18.查询已选课程时，是全部课程，不是选修课程。考虑一键选课功能

26.记录文件操作记录
30.文件只查看启用功能，文件启用不显示问题



已解决问题
    13.学生已选课程查询不完全问题。仅有一条，且查不出来
    7.个人信息中添加不可修改的权限
    6.menu增删改后，只要父菜单选中则子菜单均选中的情况
    14.做一个我的课程的选项
    12.汉语言课表 id=6的课程在搜索'大学生'时不显示。且搜索后结果没有state一栏数据
    19.控制台乱码问题
    16.定义localstorage的enableDownload，老师设定后，该文件的此变量变化
    10.文件备份问题（8.5）
    20.file的upload接口用不了（8.6）
    22.file的更新者查询不到问题（8.6）
    22.时间戳转换问题（8.7）
    26.person页面进不去问题（8.8）
    24.记录文件下载记录（8.8）
    21.设置更新头像不会更新到文件里面（8.8）
    23.文件写不进磁盘问题，文件上传失败 和 头像上传失败问题（8.8）
    25.文件下载记录查询不到问题，可以在查询时基于left join关联查询解决（8.11）
    27.更新数据库文件的同时，更新sys_user表中的avatar字段（fileController中)（8.11）
    28.文件下载记录只是当前用户的某个文件最新下载记录（8.11）
    29.更新后文件下载失败问题（8.11）
    31.添加更新菜单后需要重启请求

解决方法：
    在load方法中添加sql语句，每次重置设置
    问题26：
        在原有的fileController基础上增加checkDownload方法后，前端查询不到数据 -> 新增了UserFile的控制层，接口层加以处理 -> 前端可以接收到数据了，但是只有用户id，还查不到用户的nickname
        -> 考虑采用关联查询 -> datagrip验证关联查询可行 -> 在最后返回的对象上出现问题，返回的是多个对象，我只接收了一个 -> 使接收List<对象> -> 查询不到数据
        -> 新增FileDTO类清洗数据，并加上JsonFormat注解 格式化下载时间
        -> 只接收到用户名 -> 发现最后返回的FileDTO类中的下载时间写的是download_time，和前端接收数据的驼峰命名不相符，果断改写 -> 实现下载记录查询功能
    问题27：
        确定使用文件删除或更新文件信息时调用 -> FileMapper写SQL 根据旧文件名在sys_user表中模糊查询到用户，更新头像为新文件名/null
        -> file接口 + file接口的实现类中给旧文件名添加% -> fileController中改写一键清空、文件永久删除和更新的方法
    问题28：
        文件下载记录采用先删后增的方法插入，且删除时使用用户id（deleteByUserId），会造成虽然新增文件，但同一用户下载时，每次更新都会把一个用户的所有下载记录先删除再进行添加
        -> 采用(用户，文件)的形式进行删除，不多删也不少删 -> userFileMapper中新写了delete(userId, fileId)方法
    问题29：
        文件下载时会调用文件的url为fileUUID，在前几次的更新中，忽略了更新文件的url，借鉴问题27的解决办法进行改进
        -> fileMapper中写SQL，发现更新的url和sys_user中的avatar存储的书写格式相同，则直接照搬问题27解决方法
